# Secure Terminal based Password Manager
password-manager generates secure passwords and stores them locally by utlizing a simple numbers-based encrypting technique.
Made as a solution to most browsers storing and exporting passwords in plain text. 
Existing passwords may also be added. csv format is chosen for simplicity, and it can be updated to JSON if desired.